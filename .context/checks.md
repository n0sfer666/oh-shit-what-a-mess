# Проверки OSWaM

| Команда | Назначение |
|---------|------------|
| `cargo test` | юнит/интеграционные тесты (TDD-условие коммита) |
| `cargo clippy --all-targets -- -D warnings` | статанализ, варнинги = ошибки |
| `cargo fmt --check` | форматирование |

Условие коммита: все три зелёные.
